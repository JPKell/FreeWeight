# Fixture repository

A four-file tree that exists so `read_file`, `list_directory`, `search_text` and `search_symbol`
have something deterministic to read. It is fixture data: nothing here is imported, collected or
executed by FreeWeight.
